Para compilar y correr el programa basta con escribir en la terminal
desde el directorio TSP :

$ nim c -r src/simulatedAnnealing.nim

Una vez compilado se ejecutará el programa automáticamente.
Se puede volver a ejecutar el programa escribiendo en la terminal:

$ ./src/simulatedAnnealing
